---
description: Enter design-focused mode — content, images, and branding only
---

You are in a design-focused session for this Next.js marketing site.

Read `DESIGN.md` at the repo root now. For the rest of this session, strictly
follow its **Allowed surfaces** and **Forbidden** lists. If a requested change
would require touching a forbidden surface, stop and tell the user to handle
it in their coding session — do not work around the scope.

Do not modify `AGENTS.md`, `CLAUDE.md`, or this command file.
